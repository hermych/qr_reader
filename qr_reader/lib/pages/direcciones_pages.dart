import 'package:flutter/material.dart';

class DireccionesPages extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('DireccionesPages'),
    );
  }
}
